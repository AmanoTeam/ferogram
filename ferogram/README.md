# ferogram

The library is in development, you need to [specify the dependency from the git repository][dep-git]:

```toml
ferogram = { git = "https://github.com/AmanoTeam/ferogram" }
```

[dep-git]: https://doc.rust-lang.org/cargo/reference/specifying-dependencies.html#specifying-dependencies-from-git-repositories
